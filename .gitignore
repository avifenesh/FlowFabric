# ── Build artifacts ──
target/
ferriskey/target/

# ── Editor / IDE ──
.cursor/
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# ── Profiling / benchmarks ──
ferriskey/bench-results-*.json
ferriskey/benchmarks/
ferriskey/flamegraph.svg
*.perf.data
perf.data

# ── Internal bench result JSONs (regenerable, hardware-specific) ──
# Keep benches/results/baseline.md as the tracked summary; raw JSONs are local.
benches/results/*.json
benches/results/*/*.json
benches/benches/
benches/comparisons/*/benches/
benches/perf-invest/redis-rs-src/

# ── Team coordination (internal, not public) ──
TEAM.md
.remember/

# ── Claude / agent artifacts ──
ferriskey/.claude/

# ── Model caches (fastembed writes to $CWD-relative .fastembed_cache/) ──
.fastembed_cache/

# ── Environment / secrets ──
.env
*.env.local
# ferriskey/.env is tracked (only contains LANG=C, not secrets)
# TLS test certs in utils/tls_crts/ are self-signed test certs (safe to track)
