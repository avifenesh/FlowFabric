# OpenRouter API key — get one at https://openrouter.ai/keys
# The llm-race example does NOT auto-load this file. Export the var
# into your shell before running `submit` / `provider` / `aggregator`:
#
#   export OPENROUTER_API_KEY=sk-or-...
#
OPENROUTER_API_KEY=
