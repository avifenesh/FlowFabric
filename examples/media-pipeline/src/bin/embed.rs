//! Stub — W3 owns the full implementation.
//!
//! Embed worker: claims executions with capability `embed`, produces a
//! fixed-dim vector from the approved summary, and completes with an
//! `EmbedResult`.

fn main() {
    eprintln!("embed: stub — W3 owns the full implementation");
    std::process::exit(2);
}
