//! Stub — full implementation lands in the next commit.
//!
//! Tails the summarize execution's stream, prompts on suspend, fetches the
//! waitpoint_token from /pending-waitpoints, and delivers an HMAC-signed
//! approval signal.

fn main() {
    eprintln!("review: stub — full implementation pending");
    std::process::exit(2);
}
