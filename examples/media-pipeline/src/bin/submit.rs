//! Stub — full implementation lands in the next commit.
//!
//! Creates the 3-stage media pipeline (transcribe → summarize → embed) via
//! the FF server REST API, tails all three streams concurrently, and exits
//! with a non-zero status if any stage fails.

fn main() {
    eprintln!("submit: stub — full implementation pending");
    std::process::exit(2);
}
