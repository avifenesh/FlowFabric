//! Stub — W3 owns the full implementation.
//!
//! Summarize worker: claims executions with capability `llm`, streams LLM
//! tokens as frames, suspends for human review, and resumes on an
//! HMAC-signed `summary_approved` signal.

fn main() {
    eprintln!("summarize: stub — W3 owns the full implementation");
    std::process::exit(2);
}
