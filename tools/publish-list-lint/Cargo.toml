[package]
name = "publish-list-lint"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[package.metadata.release]
release = false

[dependencies]
toml = "0.8"
serde = { workspace = true }
